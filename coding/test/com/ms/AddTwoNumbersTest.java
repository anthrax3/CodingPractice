package com.ms;

import codeReview.Utilities.ListNode;
import org.junit.Test;

/**
 * Created by wangdong on 11/13/16.
 */
public class AddTwoNumbersTest {
  /*
  @Test
  public void addTwo() {
    ListNode l1 = getNumber("321");
    ListNode l2 = getNumber("21");

    AddTwoNumbers2 test = new AddTwoNumbers2();
    ListNode l3 = test.addTwoNumbers(l1, l2);
  }

  ListNode getNumber(String num) {
    ListNode head = new ListNode(1);
    ListNode cur = head;
    for(char n : num.toCharArray()) {
      cur.next = new ListNode(n-'0');
      cur = cur.next;
    }
    cur.next = null;
    return head.next;
  }
  */
}
